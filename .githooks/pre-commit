#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
unset DATABASE_URL DATABASE_URL_POOLED TIJARA_LOCAL_DB_PORT TIJARA_TEST_DB_PORT
unset MIX_ENV PHX_SERVER
exec python3 scripts/format-staged.py
