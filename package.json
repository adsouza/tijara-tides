{
  "name": "tijara-tides-desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:test": "node --test test/desktop/*.test.mjs",
    "desktop:icons": "tauri icon src-tauri/icons/icon.svg",
    "desktop:coverage": "node --experimental-test-coverage --test test/desktop/*.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
